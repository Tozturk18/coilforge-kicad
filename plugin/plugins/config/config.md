# config.py

Shared typed configuration model.

## Purpose

`config.py` defines `CoilConfig`, the dataclass used across UI, validation, settings, and bridge layers.

## Important Symbols

- `CoilConfig`: canonical configuration object for coil generation parameters.
